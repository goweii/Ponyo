<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorBackground"
    android:orientation="vertical"
    android:padding="5dp">

    <per.goweii.ponyo.panel.AutoSplitTextView
        android:id="@+id/tv_log_tag"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        android:alpha="0.5"
        tools:textColor="@color/colorLogDebug"
        android:textSize="11sp"
        android:typeface="monospace"
        tools:text="08:47:39 main DEBUG/LogTag" />

    <per.goweii.ponyo.panel.AutoSplitTextView
        android:id="@+id/tv_log_call"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        android:alpha="0.5"
        tools:textColor="@color/colorLogDebug"
        android:textSize="11sp"
        android:typeface="monospace"
        tools:text="Activiy.onCreate(MainActivity.kt:30)" />

    <per.goweii.ponyo.panel.AutoSplitTextView
        android:id="@+id/tv_log_msg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        tools:textColor="@color/colorLogDebug"
        android:textSize="12sp"
        android:typeface="monospace"
        tools:text="this is a log" />

</LinearLayout>